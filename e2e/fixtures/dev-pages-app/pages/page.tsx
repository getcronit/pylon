// The dev-loop e2e flips the heading text below and asserts the new text is
// served. Keep exactly ONE marker token in this file (the <h1>); a token in this
// comment would be replaced first and the rendered output would never change.
export default function Page() {
  return <h1>MARKER_V1</h1>
}
